// common String Functions (from <string.h>)

// strlen(str) → length of string

// strcpy(dest, src) → copy

// strcat(str1, str2) → concatenate (join)

// strcmp(str1, str2) → compare

// strrev(str) → reverse (not always available, compiler-dependent)

#include<stdio.h>
#include<string.h>

